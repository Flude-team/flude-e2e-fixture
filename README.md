# flude-e2e-fixture

Tiny public fixture repository used by `flude-worker`'s (DEL-B23) real end-to-end tests:
`commit_sha` pinning, `strict` mode (Doxygen warnings), and `format` selection. Not real
production code -- deliberately small and deliberately has an undocumented class added in a
later commit.
